name: I0-B export exact branch tree

on:
  push:
    branches:
      - impl/i0-b-wow-reference-fixture
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6.0.2
        with:
          persist-credentials: false
      - name: Package exact tracked tree
        shell: bash
        run: |
          git ls-files -z | tar --null -T - -czf i0-b-tree.tar.gz
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: i0-b-tree
          path: i0-b-tree.tar.gz
          retention-days: 1
          if-no-files-found: error
