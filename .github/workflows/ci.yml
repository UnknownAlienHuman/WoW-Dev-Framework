name: Native API authority checkpoint
on:
  push:
    branches: [main]
permissions:
  contents: write
concurrency:
  group: w04-native-api-authority-checkpoint
  cancel-in-progress: false
env:
  BASE_COMMIT: 4d1d733dd0c24c79753277a82da4e7ff40888212
  BASE_TREE: 83338af024157506de0a077c11dcabbd5ab659e9
  ORIGINAL_CI_BLOB: c32f5910287dbe2ffe62b97582d523cf2c91b0e5
  PATCH_SHA256: 2b41fa0e097ee9245f488314dca4ad0ada3e5651d5e0214fb65c53170cfa7a15
jobs:
  checkpoint:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: false
      - name: Admit exact patch and restore manual CI before checking
        id: admit
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test "$(git rev-parse "${GITHUB_SHA}^")" = "$BASE_COMMIT"
          xz -dc .ci/w04-api-authority.patch.xz > "$RUNNER_TEMP/w04-api-authority.patch"
          printf '%s  %s\n' "$PATCH_SHA256" "$RUNNER_TEMP/w04-api-authority.patch" | sha256sum -c -
          git restore --source="$BASE_COMMIT" --staged --worktree .github/workflows/ci.yml
          git rm -- .ci/w04-api-authority.patch.xz
          test "$(git write-tree)" = "$BASE_TREE"
          git apply --index "$RUNNER_TEMP/w04-api-authority.patch"
      - name: Install minimal compiler tools
        run: rustup toolchain install stable --profile minimal --component rustfmt --component clippy
      - name: Format changed Rust modules only
        shell: bash
        run: |
          set -euo pipefail
          rustfmt +stable --edition 2024 --config skip_children=true \
            crates/wow-project/src/disk/source_manifest/mod.rs \
            crates/wow-reference/src/native_view.rs \
            crates/wow-service/src/local/native_artifact/wire.rs \
            crates/wow-service/src/local/native_input.rs
      - name: Minimal selected-package Clippy; no tests executed
        shell: bash
        run: |
          set -euo pipefail
          rustc +stable -Vv | tee "$RUNNER_TEMP/RUST_VERSION"
          cargo +stable clippy --locked \
            -p wow-project -p wow-reference -p wow-rules -p wow-service -p wow-cli \
            --all-targets --all-features -- -D warnings 2>&1 | tee "$RUNNER_TEMP/clippy.log"
      - name: Repository policy and whitespace
        shell: bash
        run: |
          set -euo pipefail
          cargo +stable xtask check 2>&1 | tee "$RUNNER_TEMP/policy.log"
          git diff --check
          git diff --cached --check
      - name: Retain exact source and check outcomes
        if: always() && steps.admit.outcome == 'success'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/checkpoint"
          git add --all
          git write-tree > "$RUNNER_TEMP/checkpoint/CHECKED_TREE"
          git archive --format=tar -o "$RUNNER_TEMP/checkpoint/checked-source.tar" "$(cat "$RUNNER_TEMP/checkpoint/CHECKED_TREE")"
          git diff --cached --binary "$BASE_COMMIT" > "$RUNNER_TEMP/checkpoint/formatted.patch"
          printf '%s\n' "$GITHUB_SHA" > "$RUNNER_TEMP/checkpoint/STAGING_SHA"
          for file in RUST_VERSION clippy.log policy.log; do
            if test -f "$RUNNER_TEMP/$file"; then cp "$RUNNER_TEMP/$file" "$RUNNER_TEMP/checkpoint/$file"; fi
          done
          (cd "$RUNNER_TEMP/checkpoint" && sha256sum checked-source.tar > SHA256SUMS)
      - name: Upload checked blobs without moving any branch
        if: success()
        uses: actions/github-script@v8
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            const { execFileSync } = require('child_process');
            const git = (...args) => execFileSync('git', args, { encoding: 'utf8' }).trim();
            const allowed = new Set([
              '.ci/w04-api-authority.patch.xz',
              '.github/workflows/ci.yml',
              'apps/wow/NATIVE_ARTIFACT.md',
              'apps/wow/NATIVE_INPUT.md',
              'crates/wow-project/src/disk/source_manifest/mod.rs',
              'crates/wow-reference/src/native_view.rs',
              'crates/wow-rules/API_EXISTS_RULE.md',
              'crates/wow-rules/CAPABILITY_AND_COVERAGE.md',
              'crates/wow-rules/SECRET_LOCAL_RULE.md',
              'crates/wow-service/src/local/native_artifact/wire.rs',
              'crates/wow-service/src/local/native_input.rs',
            ]);
            const parent = process.env.GITHUB_SHA;
            if (git('rev-parse', 'HEAD') !== parent) throw new Error('checkout moved');
            const raw = execFileSync(
              'git',
              ['diff', '--cached', '--no-renames', '--name-status', '-z', parent],
              { encoding: 'utf8' },
            );
            const parts = raw.split('\0');
            if (parts.pop() !== '' || parts.length % 2 !== 0) {
              throw new Error('invalid staged diff record');
            }
            const entries = [];
            for (let index = 0; index < parts.length; index += 2) {
