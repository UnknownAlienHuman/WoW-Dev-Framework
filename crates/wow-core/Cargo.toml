[package]
name = "wow-core"
version = "0.1.0"
description = "Exact identity, evidence, coverage, and canonical result primitives for WoW Dev Framework"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
semver = { version = "1.0.28", features = ["serde"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
sha2 = "0.11.0"

[dev-dependencies]

[lints]
workspace = true
