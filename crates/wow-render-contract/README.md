# wow-render-contract

One narrow literal-render bridge shared by a native implementation and a Wasm
adapter. Owns DTOs, wire schema, limits and errors; no Wasm runtime or source/API
truth. See [the bridge contract](../../docs/WASM_BRIDGES.md).
