# `wow-project` E6 supporting seam

E6-B external candidates remain `semantic_candidate` / `Candidate`. `wow-project` supplies only exact retained project/source owner mapping through [`EXTERNAL_CANDIDATE_MAPPING_HANDOFF.md`](EXTERNAL_CANDIDATE_MAPPING_HANDOFF.md).

This seam does not add provider transport, fuzzy search, source discovery, current resolution, external authority, or direct service/application dependencies to `wow-project`.
