# E6-B normative fixture shapes

- `provider-session-cases.json` — descriptor, authorization, session, capability, external-state, credential, and close cases.
- `query-lifecycle-cases.json` — durable query, response-loss, continuation, result, cache, retention, and degradation cases.
- `mapping-selection-cases.json` — exact project/reference owner mapping, ambiguity, explicit selection, and rejected shortcuts.
- `context-degradation-cases.json` — exact-root context handoff, external-evidence separation, privacy, optional degradation, and fallback cases.
- `CHECKSUMS.json` — prerequisite, adapter, profile, vector, member, benchmark, and bundle freeze gate.

Implementation-dependent IDs and SHA-256 values remain null only while `implementation_state` is `not-started`.
