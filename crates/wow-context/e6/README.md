# `wow-context` E6 supporting seam

E6-B passes an exact owner-mapped root into the existing context use case under [`EXTERNAL_CANDIDATE_CONTEXT_HANDOFF.md`](EXTERNAL_CANDIDATE_CONTEXT_HANDOFF.md).

Provider prose, rank, score, unverified locators, and inferred relations remain outside `ContextSemanticPack` framework facts. No dependency on `wow-cbm` or `wow-service` is added to `wow-context`.
