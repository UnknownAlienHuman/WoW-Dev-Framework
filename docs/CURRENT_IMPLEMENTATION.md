# Current implementation state

The authoritative executable ledger is [IMPLEMENTATION_STATUS.md](IMPLEMENTATION_STATUS.md).
The annotation implementation route is [KETHO_RUST_PORT.md](KETHO_RUST_PORT.md).

This file is a routing alias, not a second status ledger. Managed source updates,
GitHub-only acquisition, optional provider retrieval, persistent channels and
semantic consumer certification must not be inferred from planned contracts.
