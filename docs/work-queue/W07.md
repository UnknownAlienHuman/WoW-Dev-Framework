# W07 — Repair and activate the thin Reference Pack builder

State: specified. Priority P1. Dependency: W06/#74 service operations. Baseline `02d893cd3b9c9dd116a3330824b9764c3d5ab919`; audit F04/#68.

## What
Make `apps/wow-reference-builder` a real tested frontend for build, validate and rebuild-compare. It currently has inactive source, an absent ReferenceAdminObjectId import and absent ReferenceAdminError::not_found call, and the wrong command/exit contract. Other ReferenceAdmin aliases do exist; do not diagnose them all as absent. Adding the package to Cargo alone does not fix the frontend.

## How and files
Rewrite the frontend in `apps/wow-reference-builder/src/{lib,main}.rs` to translate the documented commands into W06 typed service requests and safe source/staging/destination adapters. Remove obsolete status/publish/read behavior unless separately specified as a distinct future command family. Implement bounded request reads, root confinement, safe symlink/reparse handling, overlap checks, isolated staging, read-back verification and guarded finalization. Application imports only wow-service among framework crates; it owns no pack semantics, reference parsing, SQL or annotation rendering. Update its tests/README/contract examples to agree. Activate a root member only once code compiles; update the existing isolated-parser CI package selection for its transitive service dependency without weakening unrelated checks or silently treating it as an independent workspace.

## Exact documentation
- [E1 app boundary: CLI commands](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/crates/wow-service/e1/APPLICATION_BOUNDARY.md#cli-commands)
- [Exit classes](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/crates/wow-service/e1/APPLICATION_BOUNDARY.md#exit-classes)
- [Filesystem safety](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/crates/wow-service/e1/APPLICATION_BOUNDARY.md#filesystem-safety)

## Acceptance
- [ ] All three documented commands route to real service operations with explicit roots/profile/request inputs.
- [ ] Exit classes 0/2/3/4/5/6/7/8 and JSON/text semantics match the contract; malformed/duplicate arguments reject.
- [ ] Oversized input, path escape, source/output overlap, replacement failure and cancellation preserve prior valid destination and report exact cleanup state.
- [ ] No direct lower-owner dependency, shell/network execution, implicit profile or public upload/signing.
- [ ] Package is actually built by the selected workspace/CI lanes; all existing relevant tests remain enabled.
- [ ] Focused app/service tests, check, fmt and strict Clippy pass; Windows filesystem cases are separately evidenced.

Specification-only draft: code and checks NOT-RUN. No full E1 or release acceptance follows merely from workspace activation. Keep the internal builder out of the default public bundle unless a distribution profile explicitly selects it.
