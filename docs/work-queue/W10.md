# W10 — Analyze XML virtual Lua units semantically

State: specified. Priority P1. Integration dependencies: W03/#71 exact Library binding and W09/#77 selected load closure. Owner coding may proceed with explicit existing inputs. Baseline `02d893cd3b9c9dd116a3330824b9764c3d5ab919`; audit F08/#68.

## What
Extend the existing XML inline syntax path to generation-bound semantic analysis of admitted virtual Lua units. Existing XML extraction, inheritance/mixin references, handler bindings and syntax mapping are implemented; do not recreate them or claim current syntax-only coverage is semantic coverage.

## How and files
Extend `wow-emmy/src/virtual_syntax.rs` through an owned virtual semantic adapter and `wow-project/src/{xml_lua,xml_bindings}.rs`; connect the service's existing local XML routes. Reuse one compatible analyzer generation, explicit Main/Library context and admitted load/inheritance/receiver bindings. Derive virtual source identity and exact XML-to-Lua byte maps; wrappers must have explicit synthetic spans and cannot manufacture source evidence. Preserve unresolved receiver/mixin/handler/dispatch-order outcomes. Semantic facts use existing normalized Emmy types; no upstream handles cross the seam and no second Lua parser is introduced. Route mapped diagnostics and supported rule facts through ordinary file/rule selection only where capabilities actually hold.

## Documentation
- [XML: inline Lua source units](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/crates/wow-project/e2/XML_MODEL.md#inline-lua-source-units)
- [Emmy: generation boundary](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/crates/wow-emmy/AGENTS.md#project-generation-boundary)
- [Coordinates §10 span validation](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/crates/wow-emmy/SOURCE_COORDINATES.md#10-span-validation)

## Acceptance
- [ ] An admitted inline handler produces actual semantic resolution/call facts and mapped diagnostics, not syntax-only placeholders.
- [ ] UTF-8, CRLF, entity-decoded text and synthetic-wrapper offsets map correctly or explicitly reject unsupported mapping.
- [ ] Stale source/profile/analyzer identity, missing Library and ambiguous inheritance never yield clean semantic results.
- [ ] Existing named-handler links, syntax-only fallback, file selection and coverage remain honest and deterministic.
- [ ] Cancellation/budgets bound virtual units and cleanup; no duplicate semantic session or source execution.
- [ ] Focused Emmy/project/service tests, check, fmt and Clippy pass with exact identity evidence.

Specification only; code/checks NOT-RUN. Full runtime dispatch, XSD conformance and platform safety remain separate unless actually implemented and evaluated. W11 consumes the resulting normalized facts; no E2 acceptance is inferred.
