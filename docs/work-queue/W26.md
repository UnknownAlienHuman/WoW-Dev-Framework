# W26 — Executable contract/queue validation and owner-scoped acceptance ledger

State: specified. Priority P1 for validator correctness; broad acceptance runs after functional implementation. Dependencies: none for validator coding. W00/#68 defines queue inputs; package acceptance depends on the actual implemented owner and evidence, not all queue PRs as a blanket gate. Baseline `02d893cd3b9c9dd116a3330824b9764c3d5ab919`; audit F09/F10/#68.

## What
Extend the existing Rust xtask validator beyond syntax/source-policy checks, and reconcile stale machine/human implementation states with real code/evidence. Do not mistake repeated contract references for duplicate definitions, optional nulls for missing code, or passing workspace tests for product acceptance.

## How and files
Extend `tools/xtask/src/repository.rs` with focused modules for schema/contract identity, declared/actual Cargo dependency edges, owned fixtures/checksums, Markdown section links and work-queue DAG/state validation. Keep syntax/duplicate-key/size/native-policy checks. Build a definition/reference index, validate schema versions and exact identities, and report owner-scoped required-field failures without rewriting fixture bytes. Cross-check active Cargo members, deliberate independent workspaces and inactive apps against `crates/MANIFEST.json`, dependency docs and status routers. Replace obsolete no-Rust/no-workspace/bootstrap claims in ARCHITECTURE, LAUNCH_GATES, owner routers and handoff without changing normative gate requirements. Check CLI help/command registry against implemented routes, including graph publication/current-pointer behavior. Add canonical machine output plus concise human diagnostics, with stable order and bounded scans. Use this ledger to run each owner's existing focused fixtures after its code builds; defer broad cross-product matrices until their functional paths exist.

## Documentation
- [Handoff: contract/fixture validator](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/docs/IMPLEMENTATION_HANDOFF.md#contractfixture-validator)
- [Changing the dependency graph](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/crates/DEPENDENCY_GRAPH.md#changing-the-graph)
- [State vocabulary](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/docs/PROJECT_COMPLETION_MATRIX.md#state-vocabulary)

## Acceptance
- [ ] Mutated fixtures demonstrate broken schema/definition/reference/digest/dependency/link/DAG detection without false positives for legitimate references or optional nulls.
- [ ] Real active members and edges match registries; intentionally separate/inactive workspaces remain distinguishable.
- [ ] Every task has exact normative sections, scope/dependencies/acceptance and truthful state; task-only drafts cannot count as code-complete.
- [ ] Current status/help routers stop directing agents to recreate implemented owners; package/launch gates remain unpassed without exact evidence.
- [ ] Reports retain owner, file/section, expected/observed identity and actual command/head/platform; missing tools/runs are NOT-RUN.
- [ ] Focused validator tests/check/fmt/Clippy pass; no fixture auto-blessing, broad CI weakening or replacement Python tooling.

Specification only; code/checks NOT-RUN. Validator implementation can proceed alongside W01–W05; its position in the index does not mean all work must wait. Acceptance reconciliation is incremental, and full E0–E7 closure is never inferred from completing this one PR.
