# W08 — Managed source materialization without destructive updates

State: specified. Priority P2; independent of the explicit-local-input critical path. Dependency: W02/#70 bounded host intake for shared acquisition helpers. Baseline `02d893cd3b9c9dd116a3330824b9764c3d5ab919`; audit source-acquisition gap/#68.

## What
Complete missing managed-checkout creation and exact GitHub fallback materialization under the existing auto/prompt/never policy. Existing `check-source` and guarded fast-forward are real code and must be reused, not rewritten or replaced with reset/clean.

## How and ownership
Extend existing Rust source/manifest maintenance owners in tools/xtask, with explicit host inputs reusable by W03/W06. Resolve flavor and moving selector once per operation; record immutable commit/tree and per-file manifest digests. Prefer an explicitly selected local checkout; use only configured authorized HTTPS fallback. Clone missing managed roots into isolated staging and admit only a verified source tree. Read every fallback blob from the same resolved revision with finite file/count/byte/network budgets. Implement durable acquisition/update receipts and reconciliation of retained locks/partial staging; unknown outcomes are not a reason to delete locks or blindly retry. Dirty/diverged/unexpected-origin/operator-owned branches reject mutation. Do not run repository hooks, Lua, submodules, package managers, Git-configured commands or source scripts. No credentials in manifests, logs, commits or prompts.

## Exact documentation
- [Source updates: commands and authorization](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/docs/SOURCE_CHECKOUT_UPDATES.md#commands-and-authorization)
- [One observation, one guarded transition](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/docs/SOURCE_CHECKOUT_UPDATES.md#one-observation-one-guarded-transition)
- [Results and interrupted updates](https://github.com/UnknownAlienHuman/WoW-Dev-Framework/blob/02d893cd3b9c9dd116a3330824b9764c3d5ab919/docs/SOURCE_CHECKOUT_UPDATES.md#results-and-interrupted-updates)

## Acceptance
- [ ] auto/prompt/never produce the documented distinct mutation/authorization outcomes; never is read-only.
- [ ] Missing managed checkout and configured GitHub fallback materialize one digest-verified revision; no mixed branch reads.
- [ ] Dirty, diverged, wrong-origin, unsafe-path and unexpected branch cases preserve operator data.
- [ ] Interrupted clone/update reconciles exact receipts and staging; no reset/clean/blind lock deletion.
- [ ] Offline checks report unverified-current, not latest; token and source-text privacy are preserved.
- [ ] Focused Rust source-policy/manifest tests and fmt/check/Clippy pass; network/platform cases actually not run remain explicit.

Specification only; code/checks NOT-RUN. This task adds no scheduler/daemon or implicit provider. An explicit local source remains fully usable while this lane is unfinished.
