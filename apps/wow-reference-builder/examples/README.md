# `wow-reference-builder` E1 fixtures

- `cli-cases.json` freezes command shapes, explicit arguments, service-operation routing, exit-code classes, dependency boundaries, path safety, cancellation, and atomic-finalization behavior.
- `CHECKSUMS.json` freezes exact bytes before the first Rust implementation commit.

Fixtures never authorize network access, shell execution, editor mutation, source execution, signing, upload, publication, or CI.
