# E5-B CLI fixture shapes

- `cli-cases.json` — command-to-service mapping and rejected shortcuts.
- `stdout-exit-cases.json` — output/status/authorization/holdout/submission exit mappings.
- `CHECKSUMS.json` — service, command, profile, vector, and member freeze gate.