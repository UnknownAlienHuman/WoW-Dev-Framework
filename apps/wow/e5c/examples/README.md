# E5-C CLI fixture shapes

- `cli-cases.json` — command-to-service mapping and rejected selector/credential/effect shortcuts.
- `stdout-exit-cases.json` — exact output, scoped state, nonclaim, and exit-code mappings.
- `CHECKSUMS.json` — service, command, platform, output, security, and member freeze gate.