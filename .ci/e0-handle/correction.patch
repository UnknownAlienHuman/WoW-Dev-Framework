diff --git a/crates/wow-core/tests/e0_handle_conformance.rs b/crates/wow-core/tests/e0_handle_conformance.rs
index 69ed6511d571973f48dd3896d3cf797f9b23f844..b531c63744e2874da4ba3a92c399bc403f72fc4a 100644
--- a/crates/wow-core/tests/e0_handle_conformance.rs
+++ b/crates/wow-core/tests/e0_handle_conformance.rs
@@ -110,7 +110,10 @@ fn handle_004_005_006_011_rejects_invalid_path_span_digest_and_host_fields() ->
     for digest in ["sha256:00", "sha1:00", "PRIVATE_MARKER"] {
         let mut value = fixture()?;
         value["content_digest"] = digest.into();
-        let error = digest.parse::<ContentDigest<SourceContent>>().expect_err("invalid digest");
+        let error = match digest.parse::<ContentDigest<SourceContent>>() {
+            Ok(_) => return Err("invalid digest must not be admitted".into()),
+            Err(error) => error,
+        };
         assert!(matches!(error.code(), CoreErrorCode::InvalidDigest | CoreErrorCode::UnsupportedDigestAlgorithm));
         assert!(serde_json::from_value::<SourceHandle>(value).is_err());
     }
diff --git a/crates/wow-core/tests/handle/support.rs b/crates/wow-core/tests/handle/support.rs
index 7111451913202acbec186ba9e1a307ab82b3c3ab..cb66d8679bea1edf496838f73521488015a09f61 100644
--- a/crates/wow-core/tests/handle/support.rs
+++ b/crates/wow-core/tests/handle/support.rs
@@ -1,5 +1,4 @@
 use std::error::Error;
-use std::fmt::Debug;
 
 use serde::de::DeserializeOwned;
 use serde_json::Value;
@@ -52,10 +51,13 @@ pub fn reseal(mut value: Value) -> TestResult<SourceHandle> {
     Ok(serde_json::from_value(value)?)
 }
 
-pub fn assert_error<T: Debug>(
+pub fn assert_error<T>(
     result: CoreResult<T>, expected: CoreErrorCode, field: &str,
 ) -> TestResult {
-    let error = result.expect_err("invalid source handle must not be admitted");
+    let error = match result {
+        Ok(_) => return Err("invalid source handle must not be admitted".into()),
+        Err(error) => error,
+    };
     assert_eq!(error.code(), expected);
     assert_eq!(error.field_path(), Some(field));
     error.validate()?;
