diff --git a/crates/wow-project/src/load/xml_index/inline.rs b/crates/wow-project/src/load/xml_index/inline.rs
index e1b9a51c900db56e7b95bbe85ffc2446c534ccac..a498e7fb6378c2a8c6a424276b519d4eb59b84ed 100644
--- a/crates/wow-project/src/load/xml_index/inline.rs
+++ b/crates/wow-project/src/load/xml_index/inline.rs
@@ -111,7 +111,7 @@ fn append(&mut self, text: &str, start: usize, end: usize, kind: XmlLuaMapKind)
         });
         Ok(())
     }
-    pub(super) fn nonempty(&self) -> bool { !self.text.trim().is_empty() }
+    pub(super) fn nonempty(&self) -> bool { self.text.bytes().any(|byte| !matches!(byte, b' ' | b'\t' | b'\r' | b'\n')) }
     pub(super) fn finish(self, owner: &str) -> ProjectResult<XmlInlineLua> {
         let content_digest = crate::identity::source_digest(self.text.as_bytes());
         let digest: ContentDigest<CanonicalResult> = crate::identity::canonical_digest(
diff --git a/crates/wow-project/src/load/xml_index/mod.rs b/crates/wow-project/src/load/xml_index/mod.rs
index 220f69e32887d00c6d24ab898af545a2dea3aeb5..8e0b1c8995ae652520203bc8ad6abb0adaec9cb6 100644
--- a/crates/wow-project/src/load/xml_index/mod.rs
+++ b/crates/wow-project/src/load/xml_index/mod.rs
@@ -148,11 +148,9 @@ pub const fn digest(&self) -> ContentDigest<CanonicalResult> { self.digest }
     pub fn element(&self, occurrence_id: &str) -> Option<&XmlElementRecord> {
         self.elements.iter().find(|element| element.occurrence_id == occurrence_id)
     }
-    #[must_use]
     pub fn declarations(&self) -> impl Iterator<Item = &XmlElementRecord> {
         self.elements.iter().filter(|element| element.declaration.is_some())
     }
-    #[must_use]
     pub fn scripts(&self) -> impl Iterator<Item = &XmlElementRecord> {
         self.elements.iter().filter(|element| element.script.is_some())
     }
