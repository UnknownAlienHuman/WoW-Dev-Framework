--- a/crates/wow-project/src/update.rs
+++ b/crates/wow-project/src/update.rs
@@ -112,7 +112,7 @@
     }
 
     #[must_use]
-    pub fn target_libraries(mut self, libraries: Vec<LuaWorkspaceSnapshot>) -> Self {
+    pub fn with_target_libraries(mut self, libraries: Vec<LuaWorkspaceSnapshot>) -> Self {
         self.target_libraries = libraries;
         self
     }
--- a/crates/wow-project/src/snapshot.rs
+++ b/crates/wow-project/src/snapshot.rs
@@ -14,7 +14,7 @@
 use crate::configuration::PROJECT_SNAPSHOT_SCHEMA_VERSION;
 use crate::identity::{canonical_digest, canonical_id};
 use crate::{
-    ProjectAnalyzerBinding, ProjectAnalyzerCapabilityState, ProjectConfiguration, ProjectError,
+    ProjectAnalyzerBinding, ProjectConfiguration, ProjectError,
     ProjectErrorCode, ProjectFileId, ProjectFileRecord, ProjectGenerationCandidate, ProjectPhase,
     ProjectResult, ProjectSourceRegistry,
 };
