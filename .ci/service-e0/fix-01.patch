--- a/crates/wow-service/src/operation.rs
+++ b/crates/wow-service/src/operation.rs
@@ -117,7 +117,7 @@
         };
         if entry.request_digest.as_ref() != request_digest
             || entry.kind != kind
-            || !matches!(entry.state, RegistryState::InProgress)
+            || !matches!(&entry.state, RegistryState::InProgress)
         {
             return Err(ServiceError::for_operation(
                 ServiceErrorCode::InternalContractViolation,
@@ -139,7 +139,7 @@
         if entries.get(operation_id.as_str()).is_some_and(|entry| {
             entry.request_digest.as_ref() == request_digest
                 && entry.kind == kind
-                && matches!(entry.state, RegistryState::InProgress)
+                && matches!(&entry.state, RegistryState::InProgress)
         }) {
             entries.remove(operation_id.as_str());
         }
--- a/crates/wow-service/src/presentation.rs
+++ b/crates/wow-service/src/presentation.rs
@@ -77,7 +77,7 @@
     let display_root_ids = nodes
         .keys()
-        .filter(|node_id| !children.contains(node_id.as_ref()))
+        .filter(|node_id| !children.contains(node_id.as_ref() as &str))
         .cloned()
         .collect::<Vec<_>>();
--- a/crates/wow-service/src/service.rs
+++ b/crates/wow-service/src/service.rs
@@ -501,7 +501,7 @@
             || component
                 .capabilities()
                 .values()
-                .any(|state| *state == CapabilityState::Partial)
+                .any(|state| *state != CapabilityState::Available)
     }) {
         ServiceSemanticStatus::Partial
